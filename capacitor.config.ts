import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fansbook.app',
  appName: 'fansbook',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
